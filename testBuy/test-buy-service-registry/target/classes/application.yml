server:
  port: 8889
spring:
  application:
    name: service-registry
eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8888/eureka/
  instance:
    hostname: eureka_2
  server:
    enable-self-preservation: false
